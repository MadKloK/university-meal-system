#include "../include/student.hpp"
#include <iostream>

explicit Student::Student(int user_id, const std::string& student_id, const std::string& name, const std::string& email, float balance, bool is_active) {
    
}
